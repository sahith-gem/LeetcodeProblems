class Solution {
   public int[] searchRange(int[] nums, int target) {
        int start = 0;
        int end = nums.length-1;
        int first = binSearch(int[] nums,int target,int index);
        int second = binSearch(int[] nums,int target,boolean index);

    }
    public int binSearch(int[] nums,int target,boolean findindex){
        int start = 0;
        int end = nums.length-1;
        int mid=(start+end-start)/2;
        int ans = -1;
        while(start<end){
            if(target<mid){
                end=mid-1;
            }
            else if(target>mid){
                start = mid+1;
            }
            else{
                ans = mid;
                if(findindex){
                    
                }

            }
        }
    }
    //this function just retns the index value of target
  
    

}
